{
  "name": "locigram",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/connectors/*",
    "packages/connectors/qbo"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "migrate": "bun run --cwd packages/db migrate"
  }
}
